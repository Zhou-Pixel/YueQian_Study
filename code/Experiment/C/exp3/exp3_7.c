#include <stdio.h>

int main(int argc, char const *argv[])
{
    /* float(a[3]);
    float(b[2])[3];
    float c = 2.2, *p;

    a[2] = c;//正确
    a = c;//错误，a是地址，c是变量
    scanf("%f", &a);//错误，类型不同
    printf("%f", a[3]);//错误，越界访问了
    b[1][2] = a[2];//正确，数组元素的赋值
    b[1] = a;//数组的地址不可修改
    p = c;//错误，p存的应该是地址不是float型变量
    p = a;//正确 */
    return 0;
}
